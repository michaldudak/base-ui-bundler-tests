{
  "name": "bun-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun ./build.ts",
    "prebuild": "rimraf dist index.js index.js.map",
    "typecheck": "tsc --noEmit",
    "analyze-bundle": "node --experimental-strip-types --disable-warning=ExperimentalWarning ../scripts/analyze-bundle.ts --name Bun --bundle dist/index.js"
  },
  "dependencies": {
    "@base-ui/react": "catalog:",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "rimraf": "latest",
    "typescript": "latest"
  }
}
